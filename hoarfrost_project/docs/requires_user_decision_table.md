# Таблица решений REQUIRES_USER_DECISION

Назначение документа: зафиксировать решения, без которых нельзя безопасно переводить текущий `kod-na-22.05.2026-v1.md` в рабочий Python-проект для Colab/Jupyter. До подтверждения этих пунктов код должен либо использовать только уже подтверждённые формулы, либо возвращать статус `REQUIRES_USER_DECISION`.

## Блокирующие решения для `makkonen_model.py`

| ID | Область | Текущее состояние | Вопрос к пользователю | Варианты ответа | Рекомендованный статус в коде до решения |
|---|---|---|---|---|---|
| M-01 | Численные входы case_01 | `input_case_01_numeric_registry.md` сейчас выглядит как шаблон, реальные числа есть в `variables_case_01_registry.md.md`. | Подтвердить, что реальные численные значения из `variables_case_01_registry.md.md` нужно перенести в `input_case_01_numeric_registry.md` и после этого считать именно `input_case_01_numeric_registry.md` единственным источником чисел для `case_01`? | **Решение: да.** Перенести реальные численные значения и считать `input_case_01_numeric_registry.md` единственным численным источником для `case_01`. | Решено; можно собирать `config/constants.py` после обновления реестра |
| M-02 | Перенос состояния изморози | В текущем коде есть reset при `taC > 0` или `T1C > 0`; ваше актуальное правило требует не обнулять состояние автоматически. | Полностью убрать автоматический reset `H`, `rho_t`, `mass_per_m` при положительных температурах и сохранять предыдущее состояние до явно согласованного механизма таяния/сброса? | **Решение: нет, это верный механизм reset.** Температурный reset при `taC > 0` или `T1C > 0` считается подтверждённым механизмом сброса состояния. | Решено; reset разрешён именно при `taC > 0` или `T1C > 0` |
| M-03 | Рост толщины | Формулы Макконена дают `I` и `rho`, но дискретное обновление толщины не является отдельной формулой в реестре. | Подтвердить дискретную схему роста `delta_H = I * delta_tau / rho` для положительного `I`? | **Решение: да.** Использовать `delta_H = I * delta_tau / rho` для положительного `I`. | Решено; можно реализовывать в `update_hoarfrost_state` |
| M-04 | Убыль изморози | Текущий код трактует отрицательное `I` как сублимационную убыль толщины. | Разрешить использовать отрицательное `I` для уменьшения массы/толщины слоя по аналогичной дискретной схеме? | **Решение: да.** Отрицательное `I` можно использовать для убыли массы/толщины слоя по аналогичной дискретной схеме. | Решено; можно реализовывать убыль в `update_hoarfrost_state` |
| M-05 | Средняя плотность слоя | Текущий код обновляет `rho_t` взвешиванием по толщине. | Подтвердить формулу обновления средней плотности слоя `rho_t_new = (rho_t_prev * H_prev + rho_new * delta_H_growth) / H_new` при росте слоя? | **Решение: да.** Использовать указанное взвешенное обновление `rho_t` при росте слоя. | Решено; можно реализовывать обновление `rho_t` |
| M-06 | Масса льда на единицу длины | В текущем коде используется геометрия цилиндрической оболочки. | Подтвердить расчёт массы льда на метр как `mass_per_m = (pi / 4) * (D^2 - d^2) * rho_t`, где `D = d + 2H`? | **Решение: да.** Использовать `mass_per_m = (pi / 4) * (D^2 - d^2) * rho_t`. | Решено; можно реализовывать `mass_per_m` |
| M-07 | Диаметр обледенелого цилиндра | В формулах Макконена используется `D`, а текущий код берёт `D = d + 2H`. | Подтвердить геометрическую связь `D = d + 2H`? | **Решение: да.** Использовать `D = d + 2H`. | Решено; можно вычислять `D` автоматически |
| M-08 | Коэффициент `a` в формуле (8) | В текущем коде есть `a=0.032` и `a=0.007`, но их нет в численном реестре. | Какие значения `a` использовать в `Nu_b = a * Re^0.85` и когда? | **Решение: как в коде.** `a = 0.032` для наветренной стороны (`windward`) и `a = 0.007` для подветренной стороны (`lee` / `leeward`). | Решено; зафиксировать секционные значения `a` |
| M-09 | Посекционная схема цилиндра | Текущий код делит цилиндр на `windward`, `leeward`, `upper`, `lower` и усредняет прирост через `/4`. | Разрешить посекционную схему как вычислительную архитектуру первой версии, или убрать её и считать только один эквивалентный цилиндрический баланс? | **Решение: разрешить секции.** Использовать посекционную схему цилиндра как вычислительную архитектуру первой версии. | Решено; секционная схема разрешена |
| M-10 | Формула (15) при `T_s > 0` | В `assumptions_log.md` есть ограничение применять формулу (15) только при `T_s <= 0`; текущий код также обнуляет новую плотность при `T_s > 0`. | Подтвердить: при `T_s > 0` не создавать новый слой, но старое состояние не обнулять? | **Решение: сброс изморози.** При выполнении подтверждённых температурных условий reset применяется сброс состояния изморози. | Решено; синхронизировать с M-02 |

## Решения для `goryachev_model.py`

| ID | Область | Текущее состояние | Вопрос к пользователю | Варианты ответа | Рекомендованный статус в коде до решения |
|---|---|---|---|---|---|
| G-01 | `alpha*` | В `decisions_log` подтверждено `alpha* = 1 / (A - C^2 / B)`, но текущий код местами использует `lm / AN`. | Подтвердить, что единственная допустимая формула для `alpha*`: `alpha* = 1 / (A - C^2 / B)`? | **Решение: да.** Использовать только `alpha* = 1 / (A - C^2 / B)`. | Решено; исправить `lm / AN` на подтверждённую формулу |
| G-02 | Углы | Входные углы заданы в градусах, тригонометрия требует радианы. В текущем коде часть алгебраических разностей используется в градусах. | Подтвердить правило: внешние API принимают градусы, внутри всех формул Горячева углы переводятся в радианы, а при необходимости наружу возвращаются градусы? | **Решение: да.** Внешние API принимают градусы; внутри формул Горячева углы переводятся в радианы; наружу при необходимости возвращаются градусы. | Решено; добавить явные функции преобразования и проверки |
| G-03 | Ветвь формулы (2) | В реестрах подтверждён знак `+`. | Оставить в формуле (2) только ветвь `+` без параметра выбора? | **Решение: да.** Использовать только ветвь `+` без параметра выбора. | Решено; другая ветвь запрещена |
| G-04 | Формулы (8)–(11) | Чувствительности сейчас заглушки, что соответствует текущим решениям. | Оставить формулы (8)–(11) заглушками до отдельного решения о численной/аналитической производной? | **Решение: да.** Оставить формулы (8)–(11) заглушками до отдельного решения. | Решено; `NotImplementedError` с `REQUIRES_USER_DECISION` |
| G-05 | Константа `9.84` в формуле (12) | В формуле Горячева (12) стоит `9.84`, не `g=9.81`. | Оставить в формуле (12) строго `9.84`, как в эталонном реестре? | **Решение: заменить на 9.81.** В реализации формулы (12) использовать `9.81` вместо `9.84`. | Решено; использовать `9.81` в коде и зафиксировать расхождение с исходным реестром |

## Решения для `coupling_layer.py`

| ID | Область | Текущее состояние | Вопрос к пользователю | Варианты ответа | Рекомендованный статус в коде до решения |
|---|---|---|---|---|---|
| C-01 | Подтверждённый мост | В реестрах подтверждено `q0g = rho_t(t*)`. | Использовать `q0g = rho_t(t*)` как единственный автоматический мост Макконен → Горячев в первой версии? | **Решение: да.** Использовать `q0g = rho_t(t*)` как единственный автоматический мост Макконен → Горячев в первой версии. | Решено; разрешить только `q0g = rho_t(t*)` |
| C-02 | `Fg` в формуле (12) | Текущий код использует `Fg = F` из формулы (6), но это не подтверждено как мост. | Что использовать для `Fg` в формуле (12) Горячева? | **Решение: `F` из формулы (6).** Для `Fg` в формуле (12) использовать силу `F`, рассчитанную по формуле (6) Горячева. | Решено; `eq12` можно вызывать после расчёта `F` по (6) |
| C-03 | Роль формулы (12) | В реестрах формула (12) используется диагностически для сравнения толщины. | Подтвердить, что `c` по Горячеву используется только для диагностики `H(t*)` vs `c(t*)`, а не для обратного влияния на Макконена? | **Решение: да.** `c` по Горячеву используется только для диагностики `H(t*)` vs `c(t*)`, без обратного влияния на Макконена. | Решено; только диагностика |
| C-04 | Модификация `q0` льдом | В текущем плане не подтверждена передача массы льда в `q0`. | Нужно ли изменять `q0` Горячева с учётом массы льда из Макконена? | **Решение: нет.** `q0` Горячева не изменять с учётом массы льда из Макконена. | Решено; не изменять `q0` |
| C-05 | Момент `t*` | Входной момент `t*` обозначен, но правило выбора не зафиксировано. | Как выбирать `t*` для сравнения толщин? | **Решение: конкретный timestamp.** Момент `t*` задаётся как конкретная временная метка. | Решено; `t_star` должен передаваться явно как timestamp |

## Решения для `calibration.py` и нормализации данных

| ID | Область | Текущее состояние | Вопрос к пользователю | Варианты ответа | Рекомендованный статус в коде до решения |
|---|---|---|---|---|---|
| D-01 | `ea` | В `decisions_log` есть формула `ea = (RH / 100) * e_sat(ta)`, но текущий код использует насыщение над водой. | Какую подтверждённую формулу использовать для `ea`: над льдом или над водой? | **Решение: над льдом.** Для `ea` использовать насыщение над льдом. | Решено; привести `calibration.py` к формуле над льдом |
| D-02 | `qS` | В текущем коде есть расчёт через `pvlib`, но в реестре `qS` указан как входная величина. | Использовать `qS` только как входную колонку, без расчёта через `pvlib`? | **Решение: нет, разрешить `pvlib`.** Разрешён расчёт `qS` через `pvlib`. | Решено; `pvlib` разрешён как вычислительный источник `qS` |
| D-03 | Облачность METAR | В данных есть `METAR-Yakutsk.xlsx` с колонкой `c`; правила перевода в `n_tenths` требуют подтверждения. | Как переводить облачность METAR/RP5 из колонки `c` в `n_tenths`? | **Решение: использовать уже имеющееся правило из кода.** Применять текущую реализацию cloud mapping из `kod-na-22.05.2026-v1.md`. | Решено; использовать существующее правило из кода |
| D-04 | Ток из No0036 | Текущий код заменяет ток `iideistv` из No0039 на ток из No0036 через `merge_asof`. | Подтвердить, что для расчёта `QJ` нужно брать ток из `Sensor-VL-module-3-No0036-wide-ffill.csv`, а не из No0039? | **Решение: да.** Для расчёта `QJ` брать ток из `Sensor-VL-module-3-No0036-wide-ffill.csv`. | Решено; ток брать из No0036 |
| D-05 | `merge_asof` tolerance для тока | В текущем коде tolerance равен 325 секунд. | Подтвердить допуск 325 секунд для подстановки тока из No0036? | **Решение: да.** Использовать текущий допуск `325` секунд для подстановки тока из No0036. | Решено; tolerance = 325 секунд |
| D-06 | Давление по умолчанию | В текущем коде есть `default_pressure_pa = 101325`, но это типовое значение. | Разрешить использовать `101325 Па` как fallback при отсутствии давления, или запрещать расчёт на таких строках? | **Решение: разрешить fallback.** При отсутствии давления разрешено использовать `101325 Па`. | Решено; fallback давления разрешён |
| D-07 | Заполнение ветра нулём | В текущем коде есть `fillmissingwindwithzero=True`. | Разрешить заменять пропуски скорости ветра на 0? | **Решение: да.** Разрешено заменять пропуски скорости ветра на `0`. | Решено; заполнение ветра нулём разрешено |

## Второстепенные, но важные решения для инженерной чистоты

| ID | Область | Текущее состояние | Вопрос к пользователю | Варианты ответа | Рекомендованный статус в коде до решения |
|---|---|---|---|---|---|
| Q-01 | Имена переменных | Текущий код использует компактные имена вроде `taC`, `Hm`, `rhotkgm3`; реестр использует `ta_C`, `H_t0_m`, `rho_t_t0_kg_per_m3`. | Оставить текущий стиль имён в коде или привести к snake_case из реестра? | **Решение: привести к реестру.** Имена переменных привести к snake_case и соглашениям из реестра. | Решено; привести имена к реестру |
| Q-02 | Константы в `config/constants.py` | Текущий код хранит часть констант прямо в `makkonen_model.py`. | Вынести все численные значения case_01 в `config/constants.py`, а формульные модули сделать параметризованными? | **Решение: да.** Все численные значения case_01 вынести в `config/constants.py`, формульные модули сделать параметризованными. | Решено; вынести константы |
| Q-03 | ML-ready архитектура | Цель проекта — дальнейшая ML-идентификация параметров. | Сразу хранить все промежуточные расчётные поля в итоговом DataFrame для будущего ML/calibration анализа? | **Решение: да.** Хранить промежуточные расчётные поля в итоговом DataFrame для будущего ML/calibration анализа. | Решено; сохранять промежуточные поля |
| Q-04 | Поведение при неопределённости | Нужно единообразное поведение функций. | При недостающем решении функция должна бросать `NotImplementedError`, возвращать статус-словарь или писать warning и пропускать расчёт? | **Решение: `NotImplementedError`.** При недостающем решении функция должна явно бросать `NotImplementedError`. | Решено; неопределённые части не выполнять молча |

## Минимальный набор решений перед написанием кода

Для безопасной следующей итерации достаточно ответить на:

1. `M-01`: источник чисел для `case_01`.
2. `M-02`: убрать или оставить автоматический reset.
3. `M-03`: формула роста `delta_H`.
4. `M-04`: правило убыли при отрицательном `I`.
5. `M-05`: формула обновления `rho_t`.
6. `M-06`: формула массы льда на метр.
7. `M-07`: связь `D = d + 2H`.
8. `M-08`: значение/источник коэффициента `a`.
9. `C-02`: что использовать как `Fg` в формуле (12) Горячева.
10. `D-01`: формула для `ea`.
